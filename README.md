# JobResumeMatcher

This is a Resume ↔ Job matching engine using NLP and embeddings.

## Features
- Match resume to jobs, jobs to resumes, or resume+job matching
- Returns matching skills, missing skills, match score, recommendation, and confidence
- Pre-trained embeddings, no model training required
- Front-end UI and API

## Deployment
- Docker-ready for Railway
